{
  "name": "sunset-view-retreat-availability-proxy",
  "version": "1.0.0",
  "description": "Small backend that holds OwnerRez API credentials and proxies availability/quote checks for the Home page date search. Never expose these credentials to the browser.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
