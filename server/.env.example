# Copy to .env locally for testing (never commit the real .env file).
# In Render, set these as real Environment Variables on the Web Service instead.

OWNERREZ_API_EMAIL=
OWNERREZ_API_TOKEN=
OWNERREZ_PROPERTY_ID=
ALLOWED_ORIGIN=https://the-sunset-retreat.onrender.com
